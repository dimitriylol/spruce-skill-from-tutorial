import { buildSchema } from '@sprucelabs/schema'

const saveCatValuesEmitTargetBuilder = buildSchema({
    id: 'saveCatValuesEmitTarget',
    fields: {},
})

export default saveCatValuesEmitTargetBuilder
